import { IconButton } from ".";

export default {
  title: "Components/IconButton",
  component: IconButton,
};

export const Default = {
  args: {
    style: "filled",
    stateProp: "enabled",
    className: {},
  },
};
